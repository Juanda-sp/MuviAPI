# Capturas de pruebas

Guarda aqui las capturas obligatorias del proyecto:

- `test-valid-1.png`: prueba valida en Swagger, por ejemplo `GET /movies/search`.
- `test-valid-2.png`: prueba valida, por ejemplo `GET /movies/157336`.
- `test-invalid-1.png`: prueba invalida, por ejemplo `GET /movies/search?query=`.

Estas imagenes se referencian desde el `README.md`.
